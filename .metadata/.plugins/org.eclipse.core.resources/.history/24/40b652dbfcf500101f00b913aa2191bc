<!DOCTYPE html>
<html lang="en-US" xmlns:th="http://www.thymeleaf.org">

  <head>
  	<meta charset="utf-8"/>
  	<meta name="keywords" content="Online shopping, Products"/>
  	<meta name="Author" content="Hedwig Orieba"/>
  	<meta name="viewport" content="width=device-width;initial-scale=1.0"/>
  	<meta name="description" content="An online shopping platform"/>
    <title>Taco-Cloud</title>
  </head>
  
  <body>
  	<!-- since there is no action attribute, the form shall be posted to the same url that served the form. -->
    <form autocomplete="off" method="POST" th:object="${product}">
    
    	<h3> Textile Components if any:</h3>
    	<div th:each="part: ${textiles}">
    		<input type="checkbox" th:field="*{parts}" th:value="${part.id}"/>
    		<span th:text="${part.name}">PRODUCT PART</span> 
    		<br/><br/>
    	</div>
    	
    	<h3>Technology Components of any:</h3>
    	<div th:each="part: ${technology}">
    		<input type="checkbox" th:field="*{parts}" th:value="${part.id}"/>
    		<span th:text="${part.name}">PRODUCT PART</span> 
    		<br/><br/>
    	</div>
    	
    	<h3>Health Components if any:</h3>
    	<div th:each="part: ${health}">
    		<input type="checkbox" th:field="*{parts}" th:value="${part.id}"/>
    		<span th:text="${part.name}">PRODUCT PART</span> 
    		<br/><br/>
    	</div>
    	
    	<h3>Security Components if any:</h3>
    	<div th:each="part: ${security}">
    		<input type="checkbox" th:field="*{parts}" th:value="${part.id}" />
    		<span th:text="${part.name}">PRODUCT PART</span> 
    		<br/><br/>
    	</div>
    	
    	<h3>Select your sauce:</h3>
    	<div th:each="part: ${sauce}">
    		<input type="checkbox" th:field="*{parts}" th:value="${part.id}"/>
    		<span th:text="${part.name}">PRODUCT PART</span> 
    		<br/><br/>
    	</div>
    	
    	<h3> Name your taco creation:</h3>
    	<input type="text" th:field="*{name}"/>
    	<br/><br/>
    	
    	<button type="submit">Submit Your Taco</button>
    	
    </form>
  </body>
  
</html>