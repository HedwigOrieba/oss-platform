spring:
  application:
    name: store
  datasource:
    url: jdbc:mysql://localhost:3306/store_db
    username: root
    password: password
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

